# Notebook-only dependencies — install via uv pip (not uv sync).
# mlflow metadata pins pandas<3 which conflicts with uv sync resolution,
# but uv pip installs it alongside pandas 3 without downgrading.
#
# Install once from repo root:
#   uv pip install -r notebooks/requirements-notebooks.txt
mlflow>=2.14.0
boto3>=1.34.0

# LangChain/LangGraph — AI agent framework (for news_monitor_agent.ipynb)
# NOTE: langchain-google-genai >= 3.1.0 required for Gemini thought signatures
# (tool calling with Gemini 2.0+ models requires this)
langchain>=0.3.0
langgraph>=0.2.0
langchain-google-genai>=3.1.0
tavily-python>=0.5.0
