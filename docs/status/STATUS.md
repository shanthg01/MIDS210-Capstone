# PortalPoint Status Index

**Last updated:** July 16, 2026 (news-monitoring agent wired for manual live RDS runs; Alembic `program_events` migrations applied)

The original single status tracker has been split into three focused handoff docs:

| Area | Status doc | Use it for |
|---|---|---|
| Models | [`MODEL_STATUS.md`](MODEL_STATUS.md) | Model notebooks, feature contracts, artifacts, fit-score roadmap, and open modeling questions. |
| Architecture | [`ARCHITECTURE_STATUS.md`](ARCHITECTURE_STATUS.md) | Local/cloud infrastructure, Postgres/Alembic, S3, ingest, MLflow, and operational runbook context. |
| Application | [`APPLICATION_STATUS.md`](APPLICATION_STATUS.md) | Product direction, backend routers, frontend pages, tests, and app-side blockers. |

Start with `MODEL_STATUS.md` for the current critical path. M1 (re-trained, tuned group-weighted, now writes top-three archetype memberships), M2 (two-layer offense/defense team systems with reviewed labels), M3, Gap Matching, Player Projection (Model #8 — Phase 2a next-season forecast production API default, Phase 0 v2 retained as baseline comparator), Role Fit / Playing Time, Destination-Adjusted Projection, Team Rating Projection (M6, PR #49 merged 2026-07-11), and Recommendation Engine (M7, `rec-v1.2` — now ranks on real scheme/gap/role fit plus Team Rating Projection's `delta_adj_em` as a normalized macro signal) are complete baselines on `main`. The API's `/api/recommendations` endpoint is now wired to the real engine (2026-07-15) — computes live per request from `player_team_fit_scores`/`team_rating_projections`, no longer a hardcoded stub list; see `../models/recommendation_engine_plan.md`. Program Fit is descoped from the active roadmap (product decision, 2026-07-11) — not a blocker for the Recommendation Engine or fit-score work.

For the model dependency DAG, input/output contracts, and planned run order, see
[`../models/model_dependency_graph.md`](../models/model_dependency_graph.md).

**2026-06-19/20 local refresh:** M1, M2, M3, and Gap Matching now have repeatable script reruns; local Postgres was refreshed through `player_team_fit_scores` with `1,343,050` rows containing both `scheme_fit` and `gap_match`. M1 had a critical bug (no semantic cluster reordering — labels silently scrambled on every rerun); fixed and verified. 3 player archetype labels renamed based on DB-validated `def_adj_rapm` evidence. A `players.id` portability gotcha was found and documented (see `ARCHITECTURE_STATUS.md`) — pulled `data/features/*.parquet` files can reference player IDs that don't exist in your local DB.

**2026-06-20/21 — Issue #17 (Remaining Data Loading), items 1-4:** hoopR game-level logs (`hoopr_games`/`hoopr_team_game_logs`/`hoopr_player_game_logs`, via `ingest_hoopr.py --game-logs`), 247Sports transfer-portal ingest (`transfer_portal_events` + `transfers`, via `ingest_transfers_247sports.py` — season 2026 done, 2020-2026 backfill command documented in `ARCHITECTURE_STATUS.md` but not yet run), and barttorvik roster snapshots (`roster_snapshots`/`roster_snapshot_players`, via `ingest_roster_snapshots.py` — verified on one school, full ~365-school run not yet done). Items 5-8 (derived `player_team_seasons`, roster-state features, projection output tables, Program Fit proxy) remain open. See `ARCHITECTURE_STATUS.md` for full detail and backfill commands.

**2026-06-22 — Scheme Fit + Gap Matching unified all-pairs (PR #33 portal-scope follow-ups):** Scheme Fit (`scheme-cos-v3`) and Gap Matching (`gap-cos-v3`) both rebuilt to score every eligible player×school×season pair — Scheme Fit was top-50-per-player, Gap Matching was scoped to whatever Scheme Fit pre-seeded. `player_team_fit_scores.is_portal_candidate` added (migration `c7f1a9d3e652`) as the recommendation-surface scope flag, kept in sync by `portalpoint.modeling.availability` from both the 247Sports ingest and the Gap Matching rerun — no standalone backfill script. `fit_scores.py` responses now include `is_portal_candidate`/`is_current_school`; `players.py /search` gained `available_only`. Two real bugs fixed at the new scale: Gap Matching wrote `role_fit`/`program_fit` as `0.0` instead of the `50.0` stub `overall_fit` actually assumes (wrong `overall_fit` on ~8.4M rows), and Gap Matching's "preserve existing Scheme Fit" step preloaded the whole table into one python dict (~64min at ~9.6M rows) — replaced with a per-chunk query. See `MODEL_STATUS.md` and CLAUDE.md Process Improvement TODO #5 for full detail.

**2026-06-22 — Roster baseline branch:** `roster-baseline` introduces `portalpoint.modeling.roster_baseline` as the shared roster-outlook contract for Gap Matching, Role Fit, and Team Rating Projection. Gap Matching moves to `gap-cos-v4` code semantics: historical seasons infer target roster membership from `player_season_stats(S+1)`, while the latest season uses latest `roster_snapshots` with expected-departure fallback for schools without usable snapshots. Availability remains separate via `is_portal_candidate`; fit-score responses now also expose `is_roster_baseline_member`.

**2026-06-23 — Gap Matching v4 refresh complete:** local Postgres was migrated through `f1c4a8d3e570`, then `scripts/run_gap_matching.py` refreshed `player_team_fit_scores` to 9,756,718 `gap-cos-v4` rows across 2021-2026 and wrote 16,367 `roster_baseline_members`. Stale prior `gap-cos-v3` rows from five school-seasons outside the v4 roster-baseline scope were deleted, and the runner now performs that cleanup after successful rebuilds. MLflow promoted `gap-matching-scorer` v12 to Production.

**2026-06-23 — Player Projection Model #8, Phase 0 in production + Phase 1 validated:** Phase 0 (`player-projection-shrinkage-v1`, empirical-Bayes shrinkage + Ridge value model vs. Hoop Explorer RAPM) writes 27,047 neutral rows to the new `player_projections` table, served by `GET /api/players/{id}/projection`. Phase 1 (single-season scalar Kalman filter/smoother, validation only) found and fixed a real `R_t` mis-scaling bug for count-rate skills. Two unrelated real bugs surfaced and fixed in the process: `players.position` had been hardcoded `'G'` since before a fix had landed but never been re-run; Hoop Explorer ingestion was silently dropping real RAPM columns that exist in the raw source. Full `hoopr_player_game_logs` backfill to 2020-2026 (was 2026-only) also landed, unblocking Phase 2 on the data axis. See `MODEL_STATUS.md`'s Player Projection section and `../models/player_projection_state_space_plan.md`.

**2026-06-23 to 2026-06-25 — Player Projection Phase 2a, reconciled against [Issue #37](https://github.com/shanthg01/MIDS210-Capstone/issues/37), real-data validated, beats Phase 0 on offense:** cross-season Kalman state-space layer on top of Phase 1, plus 7 reconciled gaps (A-G: shared-prior block correlation, observation-layer context adjustment, real rate projections, Phase 0 shrinkage-prior integration, archetype evaluation metadata, real `player_projections` write, validation-vs-Phase-0). Real-data result, rolling-origin held-out comparison: **Phase 2a beats Phase 0 on offense every fold (~5-6% RMSE reduction), ties on defense.** It first wrote real same-season diagnostic rows under `model_version="player-projection-phase2a-v1"` (33,540 rows), separate from Phase 0 by model version. Gap B (context adjustment) was found to *regress* accuracy on real data; later root-cause analysis found the current context regressions explain too little skill variance, so the no-context configuration remains the only enabled Phase 2a path. Two follow-on additions landed after the Issue #37 reconciliation: an 11th skill (`foul_discipline`, Phase 1/2-only — `hoopr_player_game_logs.fouls` was the one real unused offensive/defensive metric) and an offense/defense feature-set split for the value-translation model (kept despite a real, measured ~30%-relative defense-accuracy cost, for interpretability). A teammate's review of the issue surfaced and got 3 more real fixes: a test-fixture `expires_at` staleness bug in `scripts/seed_test_data.py` (masked locally by coincidental real-pipeline writes), missing `seasons` in two cache filenames, and a `season_rank`-only join-key risk (fixed by threading the real `season` value through instead). Two real performance bottlenecks (found via profiling, not guessing) cut full real-data runs from hours to ~10 minutes along the way, which is what made this session's real-data iterate-fix-rerun cycle practical at all. This entry is historical; the later `player-proj-phase2a-fcast-v1` next-season forecast entry below is now the production API default. Full record in `../models/player_projection_state_space_plan.md` §22 and [Issue #37](https://github.com/shanthg01/MIDS210-Capstone/issues/37)'s comments.

**2026-06-25 — Script consolidation + production-integration decision made, later product-overridden to Phase 2a:** `scripts/run_player_projection.py` gained `run_phase0()`/`run_phase2a()` + `--phase {0,2a,both}` (no new script) — the first real-data application of Gap A (shared-prior blending, substituted in place for the 2 validated blocks) and Gap E (real `player_archetypes`, 18,770 rows, into `explanation` only), plus brand-new MLflow tracking for Phase 2a (didn't exist before). Real bug caught before trusting any result: the first run's promotion-gate metric name (`fold3_combined_rmse`) didn't exist on Phase 0's Production run, and `maybe_promote`'s missing-metric-defaults-to-zero fallback produced a nonsensical `Δ=+inf%` false auto-promotion — manually reverted, fixed to gate on `total_resid_std` (the metric Phase 0 actually logs), reran. Phase 2a did not clear the automatic 5% MLflow gate, but after the v2 defensive-sign fix and fresh validation, the product decision is to serve Phase 2a as the richer production architecture while retaining Phase 0 v2 as the baseline comparator. Full record in `../models/player_projection_state_space_plan.md` §22.

**2026-06-29 — AWS RDS PostgreSQL 15 migration complete:** local Docker Postgres replaced with shared team RDS instance (`portalpoint-db.con8amymqi1e.us-east-1.rds.amazonaws.com`). Data migrated via `pg_dump`/`pg_restore`. `docker compose` now only needed for Redis. `modeling/io.get_sync_engine()` updated to translate `ssl=require` → `sslmode=require` for psycopg2. See `docs/aws_rds_setup.md` for teammate onboarding and `docs/status/ARCHITECTURE_STATUS.md` for full infrastructure detail.

**2026-06-30 — RDS access hardened to bastion SSH tunnel:** per-teammate static-IP security group rules (real friction — broke every time someone changed networks) replaced with a bastion EC2 host; RDS security group now only allows port 5432 from the bastion's security group (source-group rule, not CIDR), no public/per-IP access. Teammates connect via `ssh -L 5433:<rds-host>:5432 ec2-user@<bastion-ip>` and point `DATABASE_URL` at `localhost:5433`. See `docs/aws_rds_setup.md`.

**2026-07-01 — Destination Projection P0-P3 fixes + re-run:** Four issues from teammate PR review addressed. **P0 (critical):** `translate_rates_to_destination_stats()` was scaling `*_per_40` box fields by `possessions/100` instead of `minutes/40` — suppressed displayed stats ~45-50% (Dunkley 12.4 PPG source → 5.26 PPG shown); fixed, tests updated. **P1:** `_TRANSFER_TRAINING_SQL` filtered training rows by portal-entry year (`t.season`) instead of destination year (`t.season + 1`) — semantically wrong per `--train-seasons` argument docs; fixed. **P1b:** destination projection API query only sorted by `computed_at DESC`, allowing stale-season rows to override newer target-season rows; fixed with `season DESC` as primary sort. **P2:** teammate had already assembled `uncertainty_components` dict — verified, no change. **P3:** `compute_cohort_validation()` added — loops same rolling-origin CV folds, reports Spearman/RMSE per tier direction (up/same/down), position group (guard/wing/big), and usage context (high-usage-scaling-down/usage-increase); 11 unit tests; logged to MLflow. Re-run results: 454,790 rows, 2,420 training rows (↑ from 2,125), CV total_resid_std=2.892 (↓ from 2.967), MLflow v3 Staging (Δ=+2.5% vs @champion). Cohort signal: tier_down=0.402, tier_up=0.276, tier_same=0.153; wing/big slightly stronger than guard. Full record in `docs/models/destination_projection_plan.md` §20.

**2026-07-11 — Freshman Prior v2 implemented (Steps A-E, commit f7d9b6a, PR #49 branch):** All five roadmap items from the §20 plan completed in one PR. **A (audit logging):** `build_school_baselines` now returns `(result, freshman_audit)` tuple; script logs `freshman_prior_audit.json` to MLflow, warns for schools with ≥3 priors or ≥20% freshman min_pct, emits two new MLflow metrics. `_SCHOOL_SEASON_SQL` adds `s.name` so school names flow through. **B (tier-calibrated priors):** `FRESHMAN_MIN_PCT_BY_TIER={1:10,2:8,3:7,4:6}` and `FRESHMAN_RAPM_DISCOUNT_BY_TIER={1:0.72,2:0.65,3:0.60,4:0.55}` replace global constants; tier-2 values identical to old defaults, no regression. **C (elite-program flag):** `ELITE_RECRUITING_SCHOOLS` (~15 schools including Duke/Kentucky/Kansas/UNC/MSU/Arizona/UCLA etc.) matched case-insensitively; 1.5× min_pct multiplier + 0.07 reduced RAPM discount. **D (position-aware opportunity weighting):** `opportunity_factor=clamp(open_min/15, floor=1/3, cap=1.5)` scales each freshman's min_pct by how open their position slot actually is. **E (CI widening):** `analytical_ci` accepts `n_freshman_priors`; each prior adds `FRESHMAN_VARIANCE_PER_PLAYER=0.4` to combined variance. **Tests:** 46/46 pass (4 new). See `docs/models/team_rating_projection_plan.md` §20 results block for full detail. Needs rerun to apply to live 457K rows.

**2026-07-11 — Team Rating Projection rerun after PR fixes:** Refreshed all 457,345 `team_rating_projections` rows (target_season=2027) after teammate `ajaypatel-8` landed source-season context fix, candidate real-stats fix, explanation scaling fix, CI formula fix, and freshman prior. off_resid_std=2.005/def_resid_std=2.052 (stable vs first run). Spot-checks confirm: Duke top candidates all positive delta (+0.005 to +0.012, up from −0.035 before fix); Cal candidates return mixed (near-zero, driven by playing-time model projecting 1-2 expected minutes — expected behavior, not a bug). 173K rows (38%) have negative delta (avg −0.445), largely from candidates who displace better incumbents or project minimal minutes — monitoring metric, not a blocker. CI width still constant ±5.2 (known issue). MLflow `team-rating-scorer` v1 registered and `@champion` alias set (was missing after first run due to `mlflow.active_run().data.metrics` returning stale snapshot — fixed to `mlflow.get_run(run_id).data.metrics`). run_id=`8a33bdc29e334c62b8bc860e2a270afb`. PR #49 ready to merge.

**2026-07-05 — Team Rating Projection PR follow-up fixes:** Addressed review findings in `team-roster-proj-v1` before merge. API `GET /api/projections/team-rating` now honors explicit `season` and filters `expires_at > now()`; seed/API tests include active 2026 and expired 2027 regression rows. Inference now uses source-season `team_season_stats`/`roster_state_features` for observed school context instead of looking for 2027 team context rows. `returning_minutes_pct` now comes from `roster_state_features` returning/departing/open minutes JSON instead of clipping `returning_player_impact` to 1.0. Candidate counterfactual rows now use prior-season candidate position, 3PT rate, and offensive rebounding when available, keeping neutral fallbacks only for missing prior stats. Explanation attribution now divides raw feature deltas by the fitted scaler before multiplying Ridge coefficients. Analytical CI now combines offense and defense residual variance (`sqrt(2×(off_resid_std²+def_resid_std²))`), though it remains global-width until player/school-specific uncertainty is added. Docs updated to remove stale HE-label/bootstrap/2027-baseline/API-stub claims.

**2026-07-05 — Incoming freshman prior added to Team Rating Projection:** Team Rating baselines now consume `roster_state_features.class_balance` for true incoming freshmen in roster snapshots before those players have matched `player_id`s or player-season stats. Each incoming freshman receives a conservative `8.0` min_pct prior, capped at `30.0` total team min_pct, assigned to open positions from `open_minutes_by_position`, and filled from position/tier slot baselines with a `0.65` RAPM discount. Freshman prior rows are marked separately and excluded from `n_known_players`, so they improve baseline depth realism without pretending the player quality is fully observed. This is still intentionally weaker than a recruiting-rank model; it gives the next rerun a sane placeholder for a major freshman blind spot.

**2026-07-02 — Team Rating Projection M6 first real run complete:** `team-roster-proj-v1`, 457,345 rows written to `team_rating_projections` (1,253 portal candidates × 365 D1 schools, target_season=2027). Two Ridge models trained on 14 ROSTER_FEATURES vs BartTorvik adj_o/adj_d labels, 2,158 school-seasons 2021-2026. 3-fold rolling-origin CV: em_rmse 1.760/1.965/1.834, off_r2=0.973/0.970/0.976, def_r2=0.950/0.943/0.947. off_resid_std=2.008, def_resid_std=2.057. Fold 3 off/def RMSE spikes (4.8) but cancels cleanly in AdjEM (1.83) — likely 2026 RAPM coverage gaps, documented as known issue. **Two real bugs found and fixed:** (1) `slot_baselines` has tuple keys — JSON-serialized as `{str(k): v}` before MLflow `log_dict`; (2) `load_inference_data` queried `roster_baseline_members WHERE season = target_season (2027)` but data only exists through 2026 — fixed to use `prior_season`. **200× Step 6 speedup:** replaced 200-sample bootstrap CI (91M Python iterations for 457K pairs) with analytical Gaussian CI; vectorized Ridge predictions per player batch (365-school matrix predict instead of 365 individual `(1×14)` calls); replaced `iterrows()` PT index build with `to_dict('records')`. MLflow `team-rating-scorer` Staging (no prior champion). `@champion` alias not yet registered. Migration `c3a9e1f5b847` applied (adds `season`, breakdown columns, extended unique constraint). run_id=`b7deb48ffa1341e088167a0eb3df688f`. Script: `scripts/run_team_rating_projection.py`; notebook: `notebooks/models/team_rating_projection_roster_tool.ipynb`.

**2026-07-01 — Destination-Adjusted Projection first real end-to-end run complete:** `player-destination-proj-v1`, 454,790 rows written for target_season=2027. 2,125 training rows (was 5 before the season-offset bug fix: 247sports `transfers.season` = portal entry year Y, destination stats are at Y+1 — SQL used wrong season for both source and dest joins). 247Sports player matching overhauled: `_normalize_name()` (NFD accent stripping, suffix removal), position pre-filter, two-pass strict/relaxed `difflib.SequenceMatcher` thresholds, multi-season fallback — match rate 87-91% (was ~0%). CV total_resid_std=2.967 across 3 rolling-origin folds; R² is low (0.025-0.071) but expected — high inherent noise, rule-based deltas not yet fitted empirically. `estimate_usage_value_coef` always returns fallback 1.5 (zero-overlap bug not yet root-caused — listed as improvement item). MLflow `destination-projection` v2 @champion. `build_roster_state_features.py` re-run post-fix: 357 rows with real `incoming_minutes`. 65 unit tests in `tests/test_destination_projection.py`, 21 in `tests/test_ingest_transfers_247sports.py`. Full findings and improvement roadmap in `docs/models/destination_projection_plan.md` §19.

**2026-07-03 — News Monitor Agent v2 prototype complete (`notebooks/agents/news_monitor_agent_v2.ipynb`):** Working LangGraph ReAct agent that monitors 247sports, ESPN, and On3 for transfer portal and coaching news, extracts events, and dynamically updates the recommendation engine's candidate pool via direct DB writes. Architecture: `search_news` (Tavily Python client, `advanced` depth, `news` topic, weekly window, score ≥ 0.5 filter) → `classify_events_batch` (either deterministic regex or Gemini-structured-output LLM; toggle via `USE_LLM_CLASSIFIER`) → `transfer_player` / `coach_departure` action tools. `transfer_player` reuses `_normalize_name()`, `_resolve_school()`, and `SCHOOL_ALIASES` from `ingest_transfers_247sports.py` for name normalization, then does two-pass fuzzy matching (0.82 strict → 0.75 relaxed) against `players`; on a confident match sets `player_team_fit_scores.is_portal_candidate = True` for all school pairings (immediately surfacing the player in the recommendation engine's filtered candidate pool) and upserts a `transfers` row with `portal_entry_date`. `coach_departure` logs the event for manual human review — coaching changes need human judgment before any downstream model action (Model 2 scheme profiles may need re-run after a new HC is confirmed). LLM classifier uses Gemini `gemini-3.1-flash-lite` at `temperature=0` with Pydantic structured output (`ArticleClassification`) — handles ambiguous headlines that regex misses; rate limited to 12 RPM (below free-tier 15 RPM ceiling). Regex vs. LLM comparison cell included in notebook, surfacing one real disagreement on the synthetic test set (regex: `player_enters_portal` on "UNC guard weighing options"; LLM: `unknown` — LLM correct). **Next step:** wire agent into a scheduled script (`scripts/run_news_monitoring.py`) for the Airflow/GitHub Actions cron cadence described in `docs/agents/agentic_news_monitoring_plan.md`. See `docs/agents/agentic_news_monitoring_plan.md` for full rollout plan.

**2026-06-25 — Player Projection production semantics corrected to true next-season forecast:** production rows now use observed season `S` to forecast target season `S+1` under `model_version="player-proj-phase2a-fcast-v1"`; same-season `player-projection-phase2a-v2` rows remain diagnostic state estimates. The API default points at the forecast version, and explanation JSON records `source_observed_season`, `target_projected_season`, and `forecast_horizon_seasons`. Phase 2a forecast CIs now vary by player by adding propagated skill-state/source-value variance to the residual error floor instead of using a single static width for every row. Final rerun wrote 30,304 forecast rows for target seasons 2022-2027, with all rows carrying projected rates/box-score payloads. Rate payloads now source team pace through `player_season_stats` because `player_school_seasons` is empty locally; `uv run pytest` passes 199 tests.

**2026-07-11 — News Monitor Agent production scaffolding complete (PR #50, Gates 1-5 + Gate 7):** Promoted the v2 notebook prototype to a production-quality module. Key changes: (1) **Shared entity resolution module** — `_normalize_name`, `_resolve_school`, `match_player` (3-pass: 0.82 strict → 0.75 relaxed → full-roster fallback; position pre-filter + disambiguation) extracted into `src/portalpoint/modeling/entity_resolution.py`; both `ingest_transfers_247sports.py` and the agent import from the same module. (2) **Agent package** — `src/portalpoint/agents/news_monitoring/` (`state.py`, `config.py`, `extract.py`, `resolve.py`, `graph.py`, `sources/tavily.py`); notebook now imports from the module. (3) **`program_events` + `program_events_review_queue` DB tables** (migration `2547054ae5cb`) — generic event log for all agent-detected events (transfer entries, coaching changes); `coaches.tenure_end`/`departure_date` added. (4) **`cross_source_dedup` node** — `dedup_node` runs after all ReAct tool calls complete, collapsing `(event_type, player_id, from_school_id, date-window-bucket)` duplicates; graph is now `START → agent → tools* → dedup → END`. (5) **`transfer_player` 4-step pipeline** — writes `program_events` → `transfer_portal_events` ON CONFLICT COALESCE → `transfers` upsert → `sync_portal_candidate_flags()` after commit; agent feeds the authoritative availability pipeline rather than bypassing it. (6) **`scripts/run_news_monitoring.py`** CLI entrypoint (`--season`, `--window-days`, `--dry-run`, `--no-llm`). (7) **Gate 7 — coaching change stale flag** — `coach_departure` UPDATEs `team_system_profiles SET stale_flag=true, stale_reason='coaching_change'` (migration `b1d3f5a7c9e2`); `fit_score_service.get_fit_score()` queries this per school/season and populates new `FitScoreResponse.scheme_fit_stale`/`scheme_fit_stale_reason` fields. **Dependencies added:** `langchain-core`, `langchain-google-genai`, `langgraph`, `tavily-python`. **Tests:** 67 new pure unit tests (`test_entity_resolution.py` × 34, `test_news_monitoring.py` × 33), 0 regressions. Phase B/C expansion (VerbalCommits, coaching/beat-writer sources, alert.py digest writer) remains next.

**2026-07-11 — Team Rating Projection PR #49 merged to `main`; Program Fit descoped; M6 → M7 macro-signal plan written:** `team-roster-proj-v1` (457,345 rows, target_season=2027, Freshman Prior v2 applied) is now on `main`, not just a rerun on a branch. Separately, product decision: Program Fit (Issue #20) is descoped from the active roadmap for now — it was already excluded from Recommendation Engine v1's `DEFAULT_FIT_WEIGHTS` (scheme/gap/role only), so this formalizes existing practice rather than changing behavior; `fit_scores.py`'s 4-component `overall_fit` keeps `program_fit` as the 50.0 placeholder pending a separate decision on whether to reweight to 3 real components. With Team Rating Projection merged and Role Fit real, M7's two previously-documented blockers are resolved — `docs/models/recommendation_engine_plan.md` lays out wiring `team_rating_projections.delta_adj_em` into the Stage-1 rank score and Stage-2 personalized re-rank as a macro "how much does this player improve the team" signal, on top of the existing scheme/gap/role fit stack. Not yet implemented — plan only.

**2026-07-11 — Recommendation Engine `rec-v1.2` — M6 macro signal implemented and run for real, same day as the plan above:** `modeling/recommendations.py` gained `team_impact_fit()` (fixed clip±5 AdjEM → 0-100, `0` delta = neutral `50.0`) and a 4-component `DEFAULT_FIT_WEIGHTS` (scheme=0.25, gap=0.30, role=0.25, team_impact_fit=0.20). `scripts/run_recommendations.py` moved Stage 1 ranking from raw SQL to Python (`generate_top_50_candidates()`), fixed the `team_rating_projections` join, and added a school-scoped freshness check. **Real bug caught before shipping:** the plan's season-offset assumption (`trp.season = ptf.season + 1`, by analogy with Destination Projection) was wrong — checked against the live DB, `player_team_fit_scores`'s current season is 2027 (not 2026; `sync_role_fit_scores()` upserts real `role_fit` directly into the same season Scheme Fit/Gap Matching already cover there), matching `team_rating_projections` directly. Confirmed empirically before fixing: `trp.season = ptf.season` → 449,315 joined rows, `+1` → 0. **Second real bug, caught live:** `maybe_promote()` crashed on `register_model` — the script had never logged an MLflow model artifact before calling it, so this had apparently never completed a real (non-dry-run) run before this session. Fixed with a `RecEnginePyfunc` marker model, same pattern as `destination_projection.py`'s `DestProjectionPyfunc`. **Real run:** school_id=301 (Lehigh), season=2027, 8 active users, 80 rows written to `recommendations`; MLflow `recommendation-engine` v1 registered and `@champion` set (first production run). Review follow-up pins Stage 2 team impact to exactly 20%, corrects the missing-value contract, and brings the targeted test file to 46 passing tests; the original 236-test pure-unit suite was green before that follow-up. `/api/recommendations` router remains an unwired stub — separate, already-tracked gap. Full record in `docs/models/recommendation_engine_plan.md`.

**2026-07-14/15 — Frontend explainability pass (Overview/Glossary pages, tooltips, key-insight summaries, Compare header-row bug fix):** Onboarding modal trimmed (dropped jargon-first framing), new `/overview` and `/glossary` routes + nav items, `ProjectionCard` chunked into collapsible sections with hover tooltips throughout (backed by a new shared `frontend/src/constants/definitions.ts` single source of truth), key-insight summary strips added to `FitScorePage`/`ComparePage`/`PlayerProfilePage`, and a real CSS bug fixed on `ComparePage` (header row's `bgcolor: 'grey.50'` isn't redefined in the app's dark palette, so it rendered near-invisible until incidental hover state changed contrast).

**2026-07-16 — News Monitor Agent wired for manual live runs:** Closed the MVP wiring gaps blocking `scripts/run_news_monitoring.py` on shared RDS. **Migrations:** fixed `alembic/env.py` ConfigParser `%` interpolation failure on URL-encoded passwords; applied catch-up migration `c9e2a1f4b8d3` (creates `program_events` + partial unique indexes) and merge head `d7f3b2e1a904` on the team's RDS line. **DB writes:** fixed `ON CONFLICT` predicates in `resolve.py` to match partial unique indexes (`AND player_id IS NOT NULL` / `AND school_id IS NOT NULL`); `transfer_player` pipeline (`program_events` → `transfer_portal_events` → `transfers` → `sync_portal_candidate_flags`) and `coach_departure` (`program_events` + `team_system_profiles.stale_flag`) verified via 4 integration tests against live RDS. **CLI/graph:** `apply_env_file()` loads keys from `.env`; `--season`/`--window-days` wired into tools; `collect_results` node populates run summary; Tavily dynamic tool docstring fix. **Live smoke test:** `uv run python scripts/run_news_monitoring.py --window-days 1` completed successfully (0 events in a 1-day off-season window — expected, not a failure). **Still open:** scheduler (GitHub Actions/Airflow), `coaches.tenure_end`/`departure_date` admin DDL on RDS, Phase B/C sources.

**2026-07-15 — `/api/recommendations` wired to the real engine — was a hardcoded stub since the original FastAPI scaffold commit, on every branch including `rec-team-proj`/`rec-role-fit-v1`:** confirmed via git history that the router's `_STUB_SCORES` list was never replaced despite the real 2-stage engine (`modeling/recommendations.py`, `rec-v1.2`) being built, tested, and run for real by the batch script. Router now: resolves `school_id`/saved weights for the requesting user, builds the candidate pool live via `CANDIDATE_SQL` (moved from `scripts/run_recommendations.py` into `modeling/recommendations.py` as the canonical, importable-in-production location — `scripts/` isn't a packaged module per `pyproject.toml`, so the API can't import from it), runs `generate_top_50_candidates()`/`refine_to_top_10()` in-process, and returns real per-player reasoning text (tiered verdict + standout component, e.g. "Excellent overall fit — stands out most in scheme fit (91/100)."). Added an ownership check (`user_id` must match the authenticated caller) the stub never had. Empty candidate pool now returns a legitimate empty list instead of fake filler. **Schema change:** `FitComponents.program_fit` → `team_impact_fit` in the recommendation response only (the engine has no program_fit signal at all; `FitScoreResponse` for `/fit/:player_id` is untouched and keeps `program_fit`). Seeded `player_team_fit_scores` test rows and fixed `tests/test_recommendations.py`'s hardcoded `user_id=1001` (real `user_id` fixture now, `season` pinned explicitly to avoid the same global-`MAX(season)` fragility this doc's Recommendation Engine section already warns about). Live/deployable option was chosen over reading the separately-batched `recommendations` cache table specifically because no scheduler exists (checked `.github/workflows/`, `docker-compose.yml`) to keep that table fresh.

**2026-07-15 — Program Fit UI honesty pass (no backend/formula change):** Confirmed via DB query that `program_fit` is a hardcoded `50.0` on every real row (8.2M `gap-cos-v3` + 3.9M `playing-time-rotation-v2` rows, zero variation) — `gap_matching.py`'s `overall_fit` formula bakes in `W_ROLE*50.0 + W_PROG*50.0` at write time. Backend reweighting explicitly deferred (own follow-up, same class of decision as the M7 program_fit exclusion). This pass: removed the 5 fake Program Fit sub-bars from `FitScorePage` (were `stub_program_fit_breakdown()`'s per-request random numbers, not real data) and replaced with an honest "not live yet, here's what it's meant to represent" note, sourced from one shared `definitions.ts` string reused by the FitScorePage note, the Compare page's tooltip, and a new Settings page caption next to the (still live/adjustable, per product call) Program Fit weight sliders.

**2026-07-15 — Destination-adjusted projection now surfaces in the UI; per-row Role Fit Live/Placeholder accuracy fix:** `FitScorePage`'s "Player Projection" card was calling the neutral (`GET /players/{id}/projection`, no `school_id`) endpoint despite the page being 100% school-specific everywhere else — the destination-adjusted branch already existed and was tested, just never called from the frontend. Now wired (`getPlayerProjection(playerId, schoolId)`), with mode-aware header copy. Caught and fixed one real bug surfaced by this switch: destination-mode `projected_box_score` uses `_per_game` keys (`pts_per_game`, scaled to real expected minutes via `destination_projection.py`'s `translate_rates_to_destination_stats`), not neutral mode's `_per_40` keys — `ProjectionCard.tsx` only recognized `_per_40`, so destination-mode box scores silently rendered empty; fixed with a second per-game label map, selected by `projection_mode`. Separately: Role Fit's Live/Placeholder chip was a blanket `LIVE_COMPONENTS` Set check (always "Placeholder") — confirmed via DB that `role_fit` is genuinely live (real, varied values) on any row where `model_version === 'playing-time-rotation-v2'`, still the `50.0` stub on `gap-cos-v3` rows. Fixed with a new `isComponentLive(component, modelVersion)` helper checked per-row against `fit.model_version`, scoped to `FitScorePage` only (Dashboard/Compare have no per-player `model_version` in their response shape — would need a new API field, flagged as a follow-up, not done here).

**2026-07-20 — Production DB connectivity: bastion SSH closed, migrated to SSM Session Manager; Phase 1 infra foundation stood up:** Start of work against `docs/production_db_connectivity_plan.md`. Real finding: the bastion's SSH port was open to `0.0.0.0/0` — the entire internet, not just the team — closed entirely and replaced with AWS SSM Session Manager port-forwarding (IAM-authenticated, per-session CloudTrail logging, no shared `.pem` key for routine access). Teammates now need a second, separate AWS credential (an IAM user in the infra account's `PortalPoint-Dev` group, distinct from their S3 bucket-account credential) plus the `session-manager-plugin` installed locally. Same session also stood up: private subnets + NAT gateway + S3 gateway endpoint, an ECR repo with a working multi-stage `Dockerfile` (found and fixed a real bug — `player_projection.py` resolves `find_repo_root()` at import time via a transitive import from `players.py`, which crashed the container until `pyproject.toml` was added to the image), an ECS task security group scoped to RDS (source-group rule, matching the existing bastion pattern), RDS Multi-AZ enabled, and a GitHub Actions OIDC deploy role (`deploy.yml`, builds/pushes to ECR on merge to `main`, ECS deploy step deliberately left commented out — no ECS cluster/service exists yet). Backend ECS Fargate hosting itself (Phase 3 of `docs/road_to_production.md`) has not started. Full command log in `docs/production_deployment_commands.md`; teammate access flow in `docs/aws_rds_setup.md`.

**2026-07-20 — Backend live on ECS Fargate; real `/ready` DB health check added; production DB connectivity plan complete:** `docs/production_db_connectivity_plan.md`'s all 6 items done — ECS Fargate service (`portalpoint-prod`/`portalpoint-backend`) behind an ALB, RDS SG scoped to the ECS task SG, `DATABASE_URL`/`JWT_SECRET` in Secrets Manager, Multi-AZ RDS, bastion demoted to break-glass-only (SSH closed, SSM Session Manager). **Real near-miss caught before calling this done:** the ALB target group's health check was configured against `/ready` per the plan, but that endpoint had never actually been built — despite being the plan's whole stated fix for the 2026-07-13 incident. Had the ECS service gone live as-is, every task would have failed health checks (404) and cycled forever. Added a real `SELECT 1`-backed `/ready` to `main.py` (returns 503 on DB failure) before the service was trusted. Also found and fixed, mid-Phase-1: the bastion's SSH port had been open to `0.0.0.0/0` — the entire internet, not just the team — closed and replaced with IAM-authenticated SSM Session Manager access (per-teammate credentials in a `PortalPoint-Dev` group in a separate infra AWS account, not the S3 bucket-owner account). GitHub Actions OIDC deploy role + `deploy.yml` also stood up (build/push to ECR + force ECS redeploy on merge to `main`, no long-lived AWS keys in GitHub secrets). Not done: autoscaling policy, CORS finalization (blocked on Phase 4 frontend hosting), CloudWatch alarms on `/ready` failures. Full command log in `docs/production_deployment_commands.md`; roadmap context in `docs/road_to_production.md`.

**2026-07-20 — Frontend live on S3+CloudFront (https://d331zwrxbrp79d.cloudfront.net); CloudWatch alarm added; Phase 5 (scheduled jobs) explicitly skipped by decision:** Continuation of the same day's production work (bastion→SSM, ECS Fargate backend — see the earlier 2026-07-20 entry above). Frontend deployed to `s3://portalpoint-frontend` behind CloudFront (`E2HF7HKH8Y1FKD`) via Origin Access Control (no public bucket access) with `/api/*` routed to the ALB at the CDN layer — the SPA needed no code change to call a separate API host, `/api/*` stayed relative. **Real prerequisite found first:** `npm run build` had apparently never been run before — `npm run dev` doesn't invoke `tsc`, so 92 pre-existing TypeScript errors across 17 files surfaced for the first time, all on one root cause (MUI's installed version no longer accepts bare `fontWeight`/`display`/`textAlign`/`maxWidth`/etc. props on `Typography`/`Box`, moved into `sx`) plus a few one-offs (3 stale MUI theme classKey overrides, one unused import, one deprecated `TextField` prop). Fixed all 92, then separately fetched and merged 51 commits from `origin/main` — 6 real merge conflicts (5 frontend files + a docs header), each a genuine feature clash (main added a `DivergingBar` comparison viz + Transfer Success row, a Dashboard card/table toggle, `personalized_fit` scoring, an agent-activity page, a playing-time override scenario panel) — and per explicit instruction, checked main's newly-merged code for the same bare-prop bug class, finding and fixing 8 more instances in main's own new code that had never been through a real build either. Verified with a full `tsc -b`/`npm run build` (0 errors) and the pure-unit pytest suite (192 passed) after the merge. Decision: **Phase 5 (scheduled jobs) explicitly skipped** — no automated-freshness need exists yet, manual ingest reruns are fine; revisit if a real user needs fresher data or the news-monitoring agent needs continuous operation during the March-August portal window. **Phase 6 (observability): only the one high-value piece done** — CloudWatch alarm `portalpoint-unhealthy-targets` (ALB `UnHealthyHostCount` ≥1, 2 evaluation periods) → SNS topic `portalpoint-alerts` → email subscription; Sentry/Prometheus/Grafana/drift-detection remain explicitly deferred. Full record in `docs/road_to_production.md` and `docs/production_deployment_commands.md`.

**2026-07-15 — Scheme Fit: fixed a real mislabel, dropped a fake sub-metric, surfaced `he_scheme_fit`:** Investigating why Scheme Fit's sub-bars disagreed with its headline score (expected — headline is cosine similarity of the 3-dim shot vector, sub-bars are an unrelated per-dimension range-normalized-difference formula on the same 3 features) surfaced three real, adjacent bugs in `modeling/scheme_fit.py`: (1) **"Ball Movement Match" was mislabeled** — it's literally `mid_range_rate`'s match score; no ball-movement/assist signal exists in this model at all. Renamed to `mid_range_match` (`score_all_seasons`, `compute_scheme_fit_ondemand`, `SchemeBreakdown` schema, frontend). (2) **"Usage Match" was a hardcoded `50.0` stub, always** — removed entirely (backend stopped writing it, frontend stopped rendering it), same "fake, no signal" class as Program Fit but previously shown as a plain bar with no Placeholder marker. (3) **`he_scheme_fit`** (a real, already-computed 6-dim HoopExplorer play-type cosine, present in the DB `breakdown` JSONB whenever both sides have HE coverage) **never reached the API at all** — `SchemeBreakdown` had no field for it. Added `he_scheme_fit`/`he_breakdown` to the schema, plus a new vectorized per-play-type breakdown (`he_scheme_breakdown()`, mirrors the existing shot-distribution breakdown) in the batch scoring path. Frontend `FitScorePage` now shows a **display-only** average of Shot Distribution + Play Type match as the Scheme Fit headline (per product decision — the underlying stored `scheme_fit`/Overall Fit/recommendation-engine ranking are untouched, still shot-distribution-only) with two clearly separated groups underneath. **Batch re-run to backfill existing rows is blocked in this environment** — `scripts/run_scheme_fit.py` needs `data/features/team_style_vectors.parquet`, which doesn't exist in this checkout (produced by `run_team_clustering.py`, not run as a side effect of this fix); `fit_score_service.py` has a fallback read (`mid_range_match` → old `ball_movement_match` key → `50.0`) so nothing breaks in the meantime. **Flagged follow-up, not done:** `he_scheme_fit` still doesn't feed `overall_fit`'s 30% scheme_fit weight anywhere outside `FitScorePage`'s own display (Compare, recommendation engine ranking) — same class of deferred decision as Program Fit's reweighting.
